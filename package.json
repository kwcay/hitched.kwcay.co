{
  "name": "hitched",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "i18next": "^15.0.1",
    "react": "^16.8",
    "react-dom": "^16.8",
    "react-i18next": "^10.0.4",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.3",
    "styled-components": "^4.1.3"
  },
  "scripts": {
    "start": "env-cmd .env yarn run react-scripts start",
    "test": "env-cmd .env yarn run react-scripts test",
    "build": "env-cmd .env.production yarn run react-scripts build",
    "predeploy": "yarn run build",
    "deploy": "gh-pages --dist build --message \"Deployed to Github pages\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "env-cmd": "^8.0.2",
    "flow-bin": "^0.92.0",
    "gh-pages": "^2.0.1"
  }
}
